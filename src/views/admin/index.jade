extends ./base
block content
    .panel
        h1 admin
        ul
            if user.adminFlag.fileServer
                li: a(href="/admin/file_servers") ファイルサーバー
            if user.adminFlag.userMng
                li: a(href="/admin/users") ユーザー管理
